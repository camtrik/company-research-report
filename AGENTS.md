# AGENTS.md

本文件适用于 `/Users/ebbi/Work/investment` 及其所有子目录。

## 项目定位

这是一个用于 AI 辅助公司分析、投资研究和报告沉淀的工作目录。代理在本目录内工作时，应优先产出可复用、可追溯、适合后续补充更新的公司研究资料。

## 目录约定

公司报告统一放在：

```text
company-reports/{ticker}-{company-name}/YYYY-MM-DD-reports.md
```

约定：

- `{ticker}` 使用股票代码，优先大写，例如 `AAPL`、`NVDA`、`TSLA`。
- `{company-name}` 使用公司英文名或常用简称，建议使用小写 kebab-case，例如 `AAPL-apple`、`NVDA-nvidia`。
- 报告文件名必须使用实际报告日期，格式为 `YYYY-MM-DD-reports.md`。
- 同一公司不同日期的报告保留为独立文件，不要覆盖历史报告。
- 如果需要补充原始材料、截图、表格或模型文件，可放在同一公司目录下，并使用清晰文件名。

## 公司报告写作要求

报告正文应优先使用中文撰写。公司名、产品名、财务科目、行业术语和引用标题可保留英文；如果用户明确要求英文或原始报告已有固定英文模板，则按用户要求或既有模板执行。

新建或更新 `YYYY-MM-DD-reports.md` 时，优先包含以下结构：

```markdown
# {TICKER} - {Company Name} Research Report

Date: YYYY-MM-DD

## Executive Summary

## Company Overview

## Business Segments

## Financial Performance

## Growth Drivers

## Competitive Position

## Risks

## Valuation Notes

## Key Questions

## Sources
```

可以根据任务需要增删章节，但报告应至少覆盖：公司做什么、收入和利润质量、增长驱动、竞争格局、主要风险、估值或市场预期、后续需要验证的问题。

## 研究与引用

- 涉及当前股价、财报、估值、管理层、新闻、监管文件、分析师预期等可能变化的信息时，必须获取最新资料后再回答或写入报告。
- 优先使用一手或高可信来源：公司 IR、SEC 文件、交易所公告、财报电话会、官方新闻稿、主流财经数据源。
- 报告中的关键事实、数字和判断应尽量附来源链接或来源说明。
- 区分事实、估算和个人判断。对假设性内容使用明确措辞，例如“假设”“可能”“需要进一步验证”。
- 不要把未经验证的市场传言写成事实。

## 分析风格

- 默认使用中文输出，除非用户指定英文或报告已有固定英文格式。
- 写作应直接、审慎、可操作，避免空泛形容词。
- 投资结论需要说明核心依据和反向风险。
- 对公司进行比较时，明确同行选择理由和比较指标。
- 对估值进行讨论时，明确使用的方法、关键假设和敏感变量。

## 文件编辑规范

- 新增报告前先检查对应公司目录是否已存在。
- 更新旧报告时保留原有有效内容，不要无故删除历史分析。
- 不要改动与当前任务无关的报告或研究文件。
- 如果同一事实在多个报告中需要更新，先说明影响范围，再进行修改。

## Context7

当用户询问库、框架、SDK、API、CLI 工具或云服务的用法、配置、迁移或调试时，使用 Context7 MCP 获取当前官方文档。即使是 React、Next.js、Prisma、Express、Tailwind、Django、Spring Boot 等常见技术，也应先查文档再回答。

使用步骤：

1. 先用 `resolve-library-id` 根据库名和用户问题解析库 ID，除非用户已经提供 `/org/project` 格式的精确 ID。
2. 根据精确匹配、描述相关性、代码片段数量、来源信誉和评分选择最佳结果。
3. 使用 `query-docs` 结合选定库 ID 和用户完整问题查询文档。
4. 基于获取到的文档回答。

不要为普通重构、从零写脚本、业务逻辑调试、代码审查或通用编程概念使用 Context7。
